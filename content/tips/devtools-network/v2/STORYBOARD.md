# DevTools Network — Storyboard (v2)

Narrative: same arc as v1 (friction → open → read → simulate → block → takeaway), now with a counter-intuitive pre-roll hook ("El bug está en la red") and a sharper layout language — `code-callout` for the key insight (Slow 3G), `quote-hero` for the takeaway. Transitions are semantic: `slide-left` when we DO something, `fade` when we REFLECT.

Format-specific `.ts` files in this folder derive from this table. See `../CREDITS.md` for image attribution.

| Tiempo        | Plan Visual (Remotion Setup)                                                                                                                | Código / Asset a Renderizar                                                                                          | Audio (Locución)                                                                                                                                     |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 00:00 - 00:01.5 | `hook` · `variant: shock` — huge mint-accent text springs in (scale 0.7 → 1) with a smaller kicker line below                              | Hero: `"El bug está en la red"` · Subtext: `"no en tu código"`                                                       | "El bug está en la red, no en tu código." (when TTS is added; today silent)                                                                          |
| 00:01.5 - 00:07.5 | `step-1` · `code-typewriter` "Va lento, ¿por qué?"                                                                                        | `// "En mi máquina vuela"` / `// en producción tarda 4 segundos`                                                     | "Tu app va lenta para los usuarios pero en tu equipo vuela: ¿servidor, red o un asset pesado?"                                                       |
| 00:07.5 - 00:13.5 | `step-2` · `code-typewriter` "Abre Network" + screenshot · transition: `slide-left` (we open something)                                   | `devtools-network/command-menu.png` + `// Cmd+Opt+J -> pestaña Network`                                              | "El tab Network registra cada petición: nombre, estado, tamaño y tiempo. Todo lo que tu página descarga, en una tabla."                              |
| 00:13.5 - 00:20.5 | `step-3` · `code-typewriter` "Lee el waterfall" + screenshot zoomed to Timing column · transition: `fade` (we read, we reflect)            | `devtools-network/timing-waterfall.png` + `// Timing: verde = TTFB · azul = descarga`                                | "La columna Timing distingue al servidor (verde) de la red (azul). Ese es tu diagnóstico inicial."                                                   |
| 00:20.5 - 00:27.5 | `step-4` · `code-callout` "Simula al usuario" + screenshot · `calloutToken: "Slow 3G"` (mint glow on the string token) · transition: `slide-left` | `devtools-network/throttling.png` + `const realUser = { network: 'Slow 3G', cache: 'disabled' };`                    | "Tú tienes fibra y caché; tus usuarios no. Activa Slow 3G y Disable cache para ver la carga real del primer visitante."                              |
| 00:27.5 - 00:33.5 | `step-5` · `code-typewriter` "Bloquea y aísla" + screenshot · transition: `slide-left`                                                    | `devtools-network/block-request.png` + `// Click derecho -> Block request`                                           | "¿Sospechas de un script de terceros? Click derecho, Block request, y comprueba si tu página sobrevive sin él."                                      |
| 00:33.5 - 00:39.5 | `step-6` · `quote-hero` — single huge phrase with mint `"…"` braces · transition: `fade` (lands the takeaway)                              | `quote: "Diagnóstico, no espectáculo."`                                                                              | "Network no es para ver requests pasar. Es tu diagnóstico de si el problema vive en el servidor, en la red, o en el peso."                           |
| 00:39.5 - 00:45.5 | Outro (handled by render pipeline; not a `VideoStep`)                                                                                       | Brand insignia + `ctaQuestion` ("¿Cuál fue el cuello de botella más absurdo que cazaste…") + follow                  | — (silence; ambient bg music continues)                                                                                                              |
