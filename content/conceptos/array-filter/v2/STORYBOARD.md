# Array.filter() — Storyboard (v2)

Narrative: clumsy old way (for loop with push) → filter as one-liner → it doesn't mutate → it chains → filter vs find (boundary) → the takeaway (declarative). Format-specific `.ts` files in this folder derive from this table.

| Tiempo        | Plan Visual (Remotion Setup)                                            | Código / Asset a Renderizar                                                                    | Audio (Locución)                                                                                                          |
| ------------- | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| 00:00 - 00:07 | `step-1` · `code-typewriter` "El problema"                              | `const activos = []; for (const u of users) { if (u.active) activos.push(u); }`                | "Para sacar un subconjunto de tus datos terminas con un bucle, un if y un push: funciona, pero esconde la intención."     |
| 00:07 - 00:13 | `step-2` · `code-typewriter` "filter al rescate"                        | `const activos = users.filter(u => u.active);`                                                 | "filter expresa lo mismo en una línea: recibe un predicado y conserva solo los elementos que devuelven true."             |
| 00:13 - 00:18 | `step-3` · `code-typewriter` "No muta, devuelve"                        | `// 'users' queda intacto · 'activos' es un array nuevo`                                       | "Y no toca el array original: devuelve uno nuevo, así te ahorras efectos secundarios difíciles de rastrear."              |
| 00:18 - 00:23 | `step-4` · `code-typewriter` "Se encadena"                              | `users.filter(u => u.active).map(u => u.name);`                                                | "Como retorna un array, lo encadenas con map o reduce para armar pipelines que se leen de corrido."                       |
| 00:23 - 00:28 | `step-5` · `code-typewriter` "filter vs find"                           | `// ¿uno solo? find · ¿el subconjunto? filter`                                                 | "Un matiz que importa: si buscas un único elemento usa find; filter es para quedarte con todo el subconjunto."            |
| 00:28 - 00:33 | `step-6` · `code-typewriter` "La idea clave"                            | `// describe el QUÉ, no el CÓMO`                                                               | "La idea de fondo: con filter describes qué quieres, no cómo recorrer el array. Eso es código declarativo."               |
| 00:33 - 00:39 | Outro (handled by render pipeline; not a `VideoStep`)                   | Brand insignia + `ctaQuestion` (default) + follow                                              | — (silence; ambient bg music continues)                                                                                   |
